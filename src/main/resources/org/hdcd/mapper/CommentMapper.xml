<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.hdcd.mapper.CommentMapper">

	<select id="read" resultType="Comment">
        SELECT
            *
        FROM comment
        WHERE board_no = #{boardNo}
    </select>
    <!-- WHERE comment_id = #{commentId -->

	<insert id="insert">
        INSERT INTO
        comment (board_no, content, writer)
        VALUES(#{boardNo},#{content},#{writer})
    </insert>

	<update id="update" parameterType="Comment">
        UPDATE COMMENT
            SET
            content = #{content}
        WHERE comment_id = #{commentId}
    </update>

	<delete id="delete" parameterType="int">
        DELETE FROM comment WHERE comment_id = #{commentId}
    </delete>

    <select id="getListWithPaging" resultType="org.hdcd.domain.Comment">
        select comment_id, board_no, content, writer, reg_date
        from comment
        where board_no = #{boardNo}
        order by comment_id asc
    </select>
</mapper>

